# Copy to .env and fill in real values
# cp .env.example .env

# Cloudflare tunnel token (from: cloudflared tunnel token jalabi-dashboard)
CLOUDFLARE_TUNNEL_TOKEN=

# Bind-mount path for uploads (Windows D: drive absolute path)
# Use forward slashes even on Windows
UPLOAD_DIR=D:/Summer2026/Dispersion-Assisted-GS-Phase-Recovery/optical_dashboard/uploads
