To: cmacphee@g.ucla.edu
Cc: yimingz0416@g.ucla.edu
Subject: Finishing the TD-GS project — can I get I1/I2 from your setup?

Hi Callen,

I'm the undergrad on the dispersive GS phase retrieval project. I want
to be straightforward: this is the last thing I need to actually finish
it. I've built the full simulation pipeline and it works cleanly on
synthetic data, but without real I1/I2 measurements I can't close the
loop and call it done.

What I built:

  - gs_core.py — full TD-GS implementation matching Solli/Gupta/Jalali
    APL 2009, six modulation formats + 6-PSK, 3D cylindrical retrieval,
    input validation, 41-check SymPy + numerical verification suite
  - gs_fno.py — Fourier Neural Operator (~180k params) that learns
    I1,I2 -> phase in function space; resolution-invariant, trained on
    multi-format synthetic data
  - Notebooks — modern physics, integrated photonics, EM pipe modes,
    Griffiths math prep, full demo pipeline with Colab upload widget

The one thing missing: real data. The upload widget in the demo notebook
is literally waiting for I1.npy and I2.npy.

All I need:

  1. I1(t) and I2(t) — any format (.npy / .mat / .csv), even a short
     calibration segment or a run with a known input
  2. Your physical D1, D2 values (ps/nm) — I'm using -695/-800 from the
     paper but your DCF may differ
  3. Signal format used (QPSK, CW, arbitrary?) so I set the right flags

If there's IP sensitivity on lab data that's completely understood —
even a noisy calibration trace would let me validate the normalization
and call this finished.

The full repo is public:
https://github.com/ColinsCoding/Dispersion-Assisted-GS-Phase-Recovery

I'm not asking for ongoing involvement — just this one bridge so the
project is real and not just a simulation. Happy to jump on a call for
15 minutes if that's easier than an email back-and-forth.

Thank you either way.

[Your name]
[Your email / phone]
UCLA ECE — Jalali Lab

Ref: Solli, Gupta & Jalali, APL 95, 231108 (2009)
